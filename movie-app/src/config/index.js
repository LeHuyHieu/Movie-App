import routes from './routes';
import { SOCIAL_BUTTONS, ROUTER_NAVBAR_AUTH } from './constants';

const config = {
    routes,
    constants: {
        social_buttons: SOCIAL_BUTTONS,
        router_navbar_auth: ROUTER_NAVBAR_AUTH,
    }
}

export default config;